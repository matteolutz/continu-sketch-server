{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist"]
    }
  },
  "globalDependencies": ["prisma", "prisma.config.ts", "tsconfig.json"]
}
